
### Default User Configuration Informations
```terminal
[root@server-01 vagrant]# cat /etc/default/useradd 
# useradd defaults file
GROUP=100
HOME=/home
INACTIVE=-1
EXPIRE=
SHELL=/bin/bash
SKEL=/etc/skel
CREATE_MAIL_SPOOL=yes
```

```terminal
cat /etc/passwd | tail -n
cat /etc/group | tail -n
```

### No loginable / Service Account Shell
```terminal
ls /sbin/nologin
```

```terminal
cat /etc/shells
```

```terminal
usermod -aG GROUP_NAME -s SHELL_NAME USERNAME
```

```terminal
groupadd -g 6000x GROUP_NAME
```

```terminal
echo "PASS_WORD" | passswd --stdin USER_NAME
```